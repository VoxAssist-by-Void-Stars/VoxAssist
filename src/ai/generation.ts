import type { IGenerator } from "../contract/types";

/** Placeholder until teammate delivers Gemini/Opus generation. */
export function createGenerator(): IGenerator {
  throw new Error(
    "src/ai/generation.ts stub — implement createGenerator() (see src/ai/README.md)",
  );
}
