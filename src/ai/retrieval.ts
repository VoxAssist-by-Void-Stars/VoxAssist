import type { IRetriever } from "../contract/types";

/** Placeholder until teammate delivers hybrid retrieval. */
export function createRetriever(): IRetriever {
  throw new Error(
    "src/ai/retrieval.ts stub — implement createRetriever() (see src/ai/README.md)",
  );
}
