import type { IVectorStore } from "../contract/types";

/** Placeholder until teammate delivers the real Atlas + Voyage store. */
export function createStore(): IVectorStore {
  throw new Error(
    "src/ai/store.ts stub — implement createStore() (see src/ai/README.md)",
  );
}
